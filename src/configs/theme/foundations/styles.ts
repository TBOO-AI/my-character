const styles = {
  global: () => ({
    body: {
      bg: '#FFEDDC',
      color: 'content.1',
      fontFamily: 'orbital',
      overflow: 'auto !important',
    },
    '#__next': {},
  }),
}

export default styles
